<% provide(:title, 'Users') %>
<h1>Users</h1>

<div class='row'>
  <div class='col-md-6 col-md-offset-3'>
    <table class='table'>
      <thead>
        <tr>
          <th>Email</th>
          <th>Name</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= link_to user.email, user %></td>
            <td><%= user.name %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
